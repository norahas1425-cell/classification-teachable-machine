# classification-teachable-machine
Image classification model trained with Google Teachable Machine, exported to TensorFlow/Keras, with a Python script for loading the model and predicting image classes.
